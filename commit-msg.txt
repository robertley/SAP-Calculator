feat: fix multiple entities, replay calc packs, and selection sorting

* **Roloway Monkey**: Fixed ability spawn pool to accurately target the specific Dangers & Useful subset instead of the general pool for tiers 2 and 3.
* **Cocoa Bean**: Changed the transform target pool to a specific explicit list of summon pets instead of capturing all summon pets via getSummonPetNames.
* **Dunkleosteus**: Updated ailment transferring to actively filter potential targets via canApplyAilment and correctly interact with Tiger triggers.
* **Abomination and Manticore**: Fixed PetTargetingRuntimeFacade.getManticoreMult() so that an Abomination copying a Manticore will accurately amplify enemy ailments.
* **Replay Integrations**:
  - Replay Parser and ReplayCalcService now consistently extract and inject Pack array mappings directly into the calculator representations.
  - Added calculatorUrl hotspots within the Replay Odds and Replay Positioning preview images. The UI now displays these as hoverable buttons, allowing users to jump directly to the battle calculator for that specific turn's state.
* **Item Selection Dialog**: 
  - Introduced a "Sort by: Default / Trigger" mode in the item selection grid.
  - Updated ItemSelectionCatalogService to process each item's tooltip, parse its specific trigger (e.g., "Start of battle", "End turn"), and fallback to "Passive" for ability grouping.
* **Tests**: Established new vitest regression cases to assert Dunkleosteus skips, Manticore ailments, replay calculator metadata extraction, correct UI trigger-sorting behavior, and random-decision-overrides limits.
