# Shared environment file for dev proxy + replay server.
# Angular dev proxy target used by config/proxy.conf.js.
REPLAY_API_TARGET=http://127.0.0.1:3000

# Replay backend credentials/config used by server/index.js.
SAP_EMAIL=you@example.com
SAP_PASSWORD=yourpassword
SAP_API_VERSION=44
PORT=3000
